// Legacy Bintray publishing script intentionally disabled.
// Bintray was sunset and this project no longer publishes through it.
// Keep this file as a no-op so old local commands that still apply it do not break the build.

