plugins {
    id 'base'
}

allprojects {
    group = 'com.samurai.telegram'
    version = '0.1.0'
}

tasks.register('verifyModules') {
    dependsOn ':telegram-core-api:build', ':telegram-core-tdlib:build'
}

tasks.named('build') {
    dependsOn 'verifyModules'
}
